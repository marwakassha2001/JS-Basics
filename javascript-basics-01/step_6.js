const num1= prompt("Enter the dividend (number to be divided):");
const num2 = prompt("Enter the divisor (number to divide by):");
const result= num1 / num2;
alert("the result of division is " +result);